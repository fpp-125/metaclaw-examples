# metaclaw-examples

Community and starter examples for MetaClaw.

## Included

- `examples/obsidian-bot/`: Obsidian + LLM starter agent generated by `metaclaw wizard`.
- `examples/capsule-release/`: Minimal capsule release and verify sample.

## Quick Start

Run the Obsidian bot with any OpenAI-compatible provider.

### Gemini OpenAI-compatible endpoint

```bash
export GEMINI_API_KEY=...
# First, replace /ABS/PATH/TO/obsidian-bot/* with your local absolute paths.
metaclaw run examples/obsidian-bot/agent.claw --llm-api-key-env=GEMINI_API_KEY
```

### OpenAI endpoint

```yaml
agent:
  llm:
    provider: openai_compatible
    model: gpt-4.1-mini
    baseURL: https://api.openai.com/v1
    apiKeyEnv: OPENAI_API_KEY
```

```bash
export OPENAI_API_KEY=...
metaclaw run examples/obsidian-bot/agent.claw --llm-api-key-env=OPENAI_API_KEY
```

## Validate Examples

```bash
./scripts/validate_examples.sh
```

Notes:
- API keys are injected at runtime and are not stored in `agent.claw`.
- Example metadata index for discovery is in `registry/index/examples.json`.
