# Runtime-only secrets (never commit actual values)
#
# You can either:
# - export env vars in your shell, OR
# - copy this to `.env` and let `./chat.sh` load it.
#
# The TUI can also manage keys for you via: `/llm setup` or `/llm key`.

# Gemini (OpenAI-compatible endpoint)
GEMINI_API_KEY=

# OpenAI
OPENAI_API_KEY=

# Anthropic (Claude)
ANTHROPIC_API_KEY=

# Optional: a single shared env name used by quickstarts and fallbacks.
# If set, the TUI may use this value when a provider-specific env name isn't set.
OPENAI_FORMAT_API_KEY=

# Optional web search (Tavily)
TAVILY_API_KEY=
